apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ld-relay.fullname" . }}-test-connection"
  labels:
    {{- include "ld-relay.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ld-relay.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
