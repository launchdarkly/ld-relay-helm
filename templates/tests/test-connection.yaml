apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ld-relay-helm.fullname" . }}-test-connection"
  labels:
    {{- include "ld-relay-helm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ld-relay-helm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
